"use client";

import Button from "@/components/button/Button";

const FundingApproveButton = () => {
  return (
    <Button buttonSize="big" backgroudColor="primary">
      펀딩 승인
    </Button>
  );
};

export default FundingApproveButton;
